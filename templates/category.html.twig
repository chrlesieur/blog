{% extends 'base.html.twig' %}
{% block body %}
    <h1>{{ category }}</h1>
    {% for article in articles %}
    <h3>{{ article.title }}</h3>
    <p>{{ article.content }}</p>
    {% endfor %}
{% endblock %}