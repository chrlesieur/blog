{% extends 'base.html.twig' %}
{% block body %}
<h1>{{ category.name }}</h1>

        {% for article in articles %}
            <h1>{{ article.id}}</h1>
            <h2>{{ article.title }}</h2>
            <p>{{ article.content }}</p>
        {% endfor %}

{% endblock %}