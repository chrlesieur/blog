<form method="post" action="{{ path('tague_delete', {'id': tague.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tague.id) }}">
    <button class="btn">Delete</button>
</form>
