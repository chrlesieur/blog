{% extends 'base.html.twig' %}

{% block title %}Tague{% endblock %}

{% block body %}
    <h1>Tague</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tague.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tague.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tague_index') }}">back to list</a>

    <a href="{{ path('tague_edit', {'id': tague.id}) }}">edit</a>

    {{ include('tague/_delete_form.html.twig') }}
{% endblock %}
